{
  "name": "Clang Mirror Demo",
  "image": "ubuntu:latest",
  "postCreateCommand": "chmod +x scripts/setup_clang_mirror.sh scripts/run_clang_mirror.sh && scripts/setup_clang_mirror.sh && scripts/run_clang_mirror.sh && cmake -S . -B build && cmake --build build",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools"]
    }
  }
}
